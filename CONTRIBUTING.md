# Contributing

Use Node.js 20 or newer.

```bash
npm install
npm test
npm run build
```

Keep changes evidence-backed and small. Add one realistic fixture test for non-trivial analysis behavior. Do not add cloud calls, telemetry, an LLM dependency, or a new package when Node.js or an installed dependency is sufficient.
