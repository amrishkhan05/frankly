#!/usr/bin/env node
/** @format */

process.stdout.write(
  JSON.stringify({
    additionalContext:
      "Frankly is active. Use plan_change before uncertain work; use analyze_change at completion; apply at most one minimize_change correction; then use verify_change with actual test results.",
  }),
);
